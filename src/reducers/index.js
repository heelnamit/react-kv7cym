//index file of reducer
import {combineReucers} from 'redux';
const dataSaver= (initialState , action)=> {

  switch (action,type)
  case : ''
}

const reducers = combineReucers(
  dataSaver,
);